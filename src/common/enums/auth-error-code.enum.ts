/**
 * Machine-readable codes on auth failures the client needs to react to
 * differently than "wrong credentials" — e.g. offering a Google sign-in button.
 */
export const GOOGLE_ACCOUNT_CODE = 'google-account';
