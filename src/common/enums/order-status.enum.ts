export enum OrderStatus {
  New = 'new',
  Preparing = 'preparing',
  Out = 'out',
  Delivered = 'delivered',
}

export enum PaymentMethod {
  Card = 'card',
  Transfer = 'transfer',
  Cash = 'cash',
}

export enum DeliveryMode {
  Door = 'door',
  Landmark = 'landmark',
}
